import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ultimate.terminal',
  appName: 'Ultimate Terminal',
  webDir: 'dist'
};

export default config;
