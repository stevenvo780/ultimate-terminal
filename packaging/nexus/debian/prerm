#!/bin/bash
set -e

if systemctl is-active --quiet ultimate-terminal-nexus.service; then
    systemctl stop ultimate-terminal-nexus.service || true
fi

if systemctl is-enabled --quiet ultimate-terminal-nexus.service 2>/dev/null; then
    systemctl disable ultimate-terminal-nexus.service || true
fi

exit 0
